services:
  foloup:
    container_name: foloup-app
    build: .
    ports:
      - "${DOCKER_PORT:-3001}:3000"
    env_file:
      - .env
    environment:
      - NODE_ENV=production
    restart: unless-stopped
